# ⚠️  IMPORTANT: These Supabase keys were previously committed to git.
#    You MUST rotate them at https://supabase.com/dashboard → Project Settings → API Keys
#    Then replace the placeholder values below with the NEW keys.
 
VITE_SUPABASE_PROJECT_ID="rptjzdephwcxufofyvts"
VITE_SUPABASE_PUBLISHABLE_KEY="REPLACE_WITH_ANON_KEY_FROM_SUPABASE_DASHBOARD"
VITE_SUPABASE_URL="https://rptjzdephwcxufofyvts.supabase.co"
 
# Backend API base URL — required, app will not work without this
VITE_API_BASE_URL="http://localhost:4000"
 
# Google Maps client-side key for the Home Therapy live map + patient profile
# location preview. REPLACE the placeholder with a real key from Google Cloud
# Console. Required APIs (Maps JavaScript API, Directions API, Geolocation API);
# Distance Matrix is also recommended for future route-cost UIs.
# When unset OR placeholder, both pages render a setup notice instead of the
# map but otherwise stay functional (sidebar list, address text, etc.).
VITE_GOOGLE_MAPS_API_KEY="AIzaSyDWbthn5jVOZ64cwfSebjUj2605HI6DXVY"